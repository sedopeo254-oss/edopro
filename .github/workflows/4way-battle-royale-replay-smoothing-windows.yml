name: 4-Way Battle Royale Replay Smoothing Windows

on:
  push:
    branches:
      - work/4way-battle-royale-ec2d962-safe-20260829
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: 4way-battle-royale-replay-smoothing-${{ github.ref }}
  cancel-in-progress: true

env:
  DEPENDENCIES_BASE_URL: https://github.com/edo9300/edopro-vcpkg-cache/releases/latest/download
  DEPLOY_DIR: deploy

jobs:
  validate:
    name: Validate Battle Royale replay smoothing and protect 3v1
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Apply Battle Royale-only smoothing
        shell: bash
        run: python3 .github/scripts/apply-battle-royale-replay-smoothing.py
      - name: Run focused and multiplayer regressions
        shell: bash
        run: |
          set -euo pipefail
          c++ -std=c++17 -Wall -Wextra -Werror -I. \
            .github/tests/battle_royale_replay_smoothing_test.cpp \
            -o /tmp/battle-royale-replay-smoothing-test
          /tmp/battle-royale-replay-smoothing-test
          c++ -std=c++17 -Wall -Wextra -Werror -I. \
            .github/tests/multiplayer_replay_animation_test.cpp \
            -o /tmp/multiplayer-replay-animation-test
          /tmp/multiplayer-replay-animation-test
          c++ -std=c++17 -Wall -Wextra -Werror -I. \
            .github/tests/multiplayer_replay_policy_test.cpp \
            -o /tmp/multiplayer-replay-policy-test
          /tmp/multiplayer-replay-policy-test
          c++ -std=c++17 -Wall -Wextra -Werror -I. \
            .github/tests/multiplayer_attack_arrow_test.cpp \
            -o /tmp/multiplayer-attack-arrow-test
          /tmp/multiplayer-attack-arrow-test
          sh ./ocgcore/tests/run_multiplayer_state_tests.sh
          sh ./ocgcore/tests/run_multiplayer_field_tests.sh
          grep -Fq 'ApplyBattleRoyaleReplayPrivateDraw' gframe/client_field.cpp
          grep -Fq 'ApplyBattleRoyaleReplayPrivatePile' gframe/duelclient.cpp
          grep -Fq 'ShouldSkipLegacyTagSwap' gframe/duelclient.cpp
          grep -Fq 'RefreshPublicFieldCards' gframe/duelclient.cpp
          git diff --check
      - name: Verify 3v1 final behavior is frozen
        shell: bash
        run: |
          set -euo pipefail
          BASE=ef0b3f205cfd97b5fae6a181e1164c737affc896
          git diff --exit-code "$BASE" -- \
            gframe/multiplayer_replay_animation.h \
            gframe/multiplayer_replay_policy.h \
            gframe/multiplayer_attack_arrow.h \
            multiplayer-deck-master \
            multiplayer-effect-examples \
            ocgcore
          grep -Fq 'SummonTiming{ 22, 8, 8 }' gframe/multiplayer_replay_animation.h
          grep -Fq 'return is_replay && is_three_vs_one ? 12 : 8;' gframe/multiplayer_replay_animation.h
      - name: Publish tested source to work branch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            gframe/client_field.h \
            gframe/client_field.cpp \
            gframe/duelclient.cpp \
            gframe/battle_royale_replay_smoothing.h \
            .github/tests/battle_royale_replay_smoothing_test.cpp
          if ! git diff --cached --quiet; then
            git commit -m "Smooth Battle Royale replay turns and draws"
            git push origin HEAD:work/4way-battle-royale-ec2d962-safe-20260829
          fi

  windows:
    name: Windows x86 Battle Royale smooth client
    runs-on: windows-2022
    needs: validate
    env:
      TRAVIS_OS_NAME: windows
      BUILD_CONFIG: release
      ARCH: x86
      BUNDLED_CORE: "1"
      DXSDK_DIR: /c/d3d9sdk/
      VCPKG_ROOT: /c/vcpkg2
      VCPKG_CACHE_ZIP_URL: https://github.com/edo9300/edopro-vcpkg-cache/releases/latest/download/installed_x86-windows-static.zip
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Apply Battle Royale-only smoothing
        shell: bash
        run: python3 .github/scripts/apply-battle-royale-replay-smoothing.py
      - uses: microsoft/setup-msbuild@v3
      - name: Install v141 XP toolset
        shell: pwsh
        run: |
          Set-Location "C:\Program Files (x86)\Microsoft Visual Studio\Installer\"
          $InstallPath = "C:\Program Files\Microsoft Visual Studio\2022\Enterprise"
          $componentsToAdd = @(
            "Microsoft.VisualStudio.Component.VC.v141.x86.x64",
            "Microsoft.VisualStudio.Component.WinXP"
          )
          [string]$workloadArgs = $componentsToAdd | ForEach-Object { " --add " + $_ }
          $arguments = ('/c', "vs_installer.exe", 'modify', '--installPath', "`"$InstallPath`"", $workloadArgs, '--quiet', '--norestart', '--nocache')
          $process = Start-Process -FilePath "cmd.exe" -ArgumentList $arguments -Wait -PassThru -WindowStyle Hidden
          if ($process.ExitCode -ne 0) {
            throw "Visual Studio Installer exited with code $($process.ExitCode)"
          }
      - name: Install Premake and dependencies
        shell: bash
        run: |
          ./travis/install-premake5.sh
          ./travis/dependencies.sh
      - name: Build bundled client
        shell: bash
        run: ./travis/build.sh
      - name: Package no-UPX Battle Royale client
        shell: bash
        run: |
          set -euo pipefail
          ./travis/predeploy.sh
          mkdir -p deploy/textures deploy/expansions/script
          cp textures/field-3v1.png deploy/textures/field-3v1.png
          cp -r multiplayer-effect-examples deploy/
          cp -r multiplayer-deck-master deploy/
          cp multiplayer-effect-examples/c25880422.lua deploy/expansions/script/c25880422.lua
          cp multiplayer-deck-master/c*.lua deploy/expansions/script/
          rm -f deploy/compressed-ygopro.exe
          test -f deploy/ygopro.exe
          test -f deploy/textures/field-3v1.png
          test -f deploy/expansions/script/c153000012.lua
          test -f deploy/expansions/script/c511001152.lua
          test -f deploy/expansions/script/c511000405.lua
      - uses: actions/upload-artifact@v4
        with:
          name: edopro-4way-battle-royale-replay-smooth-windows-x86-no-upx
          path: deploy/
          if-no-files-found: error
